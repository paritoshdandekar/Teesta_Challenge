import React from 'react'
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    TextInput,
    useColorScheme,
    View,
    FlatList,
    TouchableOpacity,
    Button,
  } from 'react-native';
function MiniCard({item}) {
    return (
        <Text></Text>
    )
}

export default MiniCard
